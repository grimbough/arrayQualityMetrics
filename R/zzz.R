# ==========================================================================
# arrayQualityMetrics package initialization
# ==========================================================================
.onAttach <- function(libname, pkgname) {
   message(paste("\nThis version of arrayQualityMetrics is a completely new one.", "It temporarily does not work properly and needs to be fixed.", "Sorry for the possible problems you will meet using it.", "You can still use the release version.", sep="\n"))
 }
